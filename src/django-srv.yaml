# apiVersion: v1
# kind: Service
# metadata:
#   name: django
# spec:
#   selector:
#     app: django
#   ports:
#     - port: 8000
#       targetPort: 8000


apiVersion: v1
kind: Service
metadata:
  name: django
  namespace: default
  labels:
    app: django
spec:
  selector:
    app: django
  ports:
    - name: http
      port: 8000
      targetPort: 8000